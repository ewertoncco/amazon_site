{
  "meta": {
    "$_PARAM": [
      {
        "type": "text",
        "name": "venda"
      }
    ]
  },
  "exec": {
    "steps": {
      "name": "emit",
      "module": "sockets",
      "action": "emit",
      "options": {
        "eventName": "status_pagamento",
        "params": {
          "venda": "{{$_PARAM.venda}}"
        }
      }
    }
  }
}